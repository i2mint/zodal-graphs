.DS_Store
*.log
node_modules/
dist/
.turbo/
*.tsbuildinfo

# --- Claude / AI tooling ---
# .claude/ is tracked as shared agent resources (CLAUDE.md, the skills bridge).
# Ignore only machine-local settings, handoffs, and Claude-written system files.
.claude/settings.local.json
.claude/.system/
.claude/skills/.system/
.claude/handoffs/

# Agent scratch probe files (must never be committed)
probe*.cjs
probe*.mjs
_probe*
