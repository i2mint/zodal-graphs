/**
 * The React-free core of @zodal/graph-sigma, importable without React or sigma.
 *
 * Use `@zodal/graph-sigma/headless` for the capabilities / registry entry (graph-ui's pure
 * capability ranking) and the overlay-styling functions, without pulling sigma or React.
 */

export * from './capabilities.js';
export * from './overlay-style.js';
